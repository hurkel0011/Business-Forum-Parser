"""GUI package — customtkinter-based desktop interface.

Frames live in their own modules:
  - main_window: app shell with sidebar nav
  - dashboard_frame: stat cards + recent leads + breakdowns
  - scraper_frame: source toggles + pipeline runner + log
  - leads_frame: filtered/searched lead table + detail panel
  - settings_frame: API keys, keywords, min score, DB backup
"""
